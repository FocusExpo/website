export default {
    content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}",
    ],
    theme: {
        extend: {
            colors: {
                // Will populate from Figma later
                primary: '#000000',
                secondary: '#ffffff',
            },
            fontFamily: {
                // Will populate from Figma later
                sans: ['Inter', 'sans-serif'],
            },
        },
    },
    plugins: [],
}
